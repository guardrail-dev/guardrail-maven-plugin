import java.io.*;
 
File file = new File( basedir, "target/codegen-spring-mvc-java-" + projectVersion + ".jar" );
if ( !file.isFile() )
{
    throw new FileNotFoundException( "Could not find generated JAR: " + file );
}
